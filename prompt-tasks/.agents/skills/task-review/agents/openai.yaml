interface:
  display_name: "Task Review"
  short_description: "Review one prompt-task outcome before closing status"
  default_prompt: "Use $task-review on one task card: evaluate evidence, decide DONE/TODO/BLOCKED/DROP, and sync status in both card and index."
